console.log("Загружено characters.js");
/* export let characters = [
  { name: "Frodo", hasRing: false },
  { name: "Gendalf", hasRing: false },
  { name: "Oragorn", hasRing: false },
];

export function stealRing(characters, owner) {
  return characters.map((c) => {
    if (c.name == owner) {
      c.hasRing = true;
    } else {
      c.hasRing = false;
    }
  });
} */

export function log() {
  console.log("log");
}
